# 软件简介 
软件主要功能是通过读取模型文件 brep stp 或者手动创建几何后可以通过 opengl 渲染显示，几何使用 OCC 进行管理，还要支持用户通过鼠标编辑几何，例如 trim 等，支持选择几何点、边、面以及 part，并且后续还会对几何面进行网格剖分，最终还要引入 AI 来实现网格质量诊断与协助修复差网格信息。

# 软件架构
- include 头文件包括对外接口，不同 src 下的目录为不同模块，只能通过 include 暴露的接口进行调用

# 计划任务
1. 重新 review 并修改下 render 相关代码 包括 #file:render_scene_controller.cpp 中 render 数据构建相关是否太复杂了？ #file:render_pick_resolver.cpp mesh pick 是否没有用处，接口是否有重复的 等等包括其他 pass 等相关功能代码都要 reveiw 并修改下：
    -  review 并修复 当前 git 缓存中的数据
    - review 并修复 当前 render 模块代码
    - review 并修复 当前 pick 模块代码
    - review 并修复 当前 pass 模块代码
    - review 并修复 geo mesh render builder 代码
2. mesh generation page 与 smooth 页面太丑了，大量无异于的title、控件溢出等问题，重新设计一下界面，要求如下：
    - 界面要简洁美观，符合现代 UI 设计原则。
    - 控件布局要合理，避免控件溢出和重叠。
    - 标题和标签要清晰明确，能够准确描述控件的功能。
    - 界面要响应式，能够适应不同屏幕尺寸和分辨率。
3. 帮我输出一个设计文档到 doc 中，可以支持高效描述几何与网格数据、使其方便的与大模型进行沟通以实现如下功能：
    - 大模型支持调用我提供的接口来获取几何与网格数据的详细信息，包括但不限于点、边、面、体等基本元素的属性和关系。
    - 大模型支持调用我提供的接口来进行剖分、修改网格等操作，并且能够获取操作结果的详细信息。
    - 大模型分析某个几何网格剖分时的最佳方案，给出具体的操作步骤和参数设置建议。
    - 大模型能够根据我提供的接口来诊断网格质量问题，并且给出具体的修复建议和操作步骤，以及全自动修复网格等等。
4. 需要代码编译通过、并同步更新 doc 与 readme，代码风格要遵循 doxygen_comment_style.md。