# 软件简介 
软件主要功能是通过读取模型文件 brep stp 或者手动创建几何后可以通过 opengl 渲染显示，几何使用 OCC 进行管理，还要支持用户通过鼠标编辑几何，例如 trim 等，支持选择几何点、边、面以及 part，并且后续还会对几何面进行网格剖分，最终还要引入 AI 来实现网格质量诊断与协助修复差网格信息。

# 软件架构
- include 头文件包括对外接口，不同 src 下的目录为不同模块，只能通过 include 暴露的接口进行调用；
- 请深入理解代码后完成以下规划的任务；

# 计划任务

1. 集合渲染相关问题：拾取面后选中效果不太对，鼠标 hover 高亮效果很好，但是选中后得面比原始面要大一圈。同样也检查下其他渲染是否有类似问题，例如网格节点、网格单元的拾取与高亮显示等；

2. 针对某个面完成网格剖分后，并没有渲染对应面得网格剖分信息，请实现对应得 mesh render 相关代码，使得网格剖分信息可以正确渲染显示出来；不要和集合 GeometryPass 混在一起，网格渲染单独实现一个 MeshPass；
    - 并且完善网格剖分界面得参数设置
    - 支持设置 三角 四边形剖分
    - 支持 2D 3D 剖分
    - 剖分完成可以正常渲染出网格效果，并且支持拾取功能
    - 并且要注意渲染拾取，不要让网格剖分信息干扰到原有的几何拾取与高亮显示等功能；

3. 为 左侧得 sidebar 每个 part 增加两个开关可以显隐几何与网格，默认都是显示的；

4. 为我梳理下当前 render 代码得整体架构生成一个文档到 doc 中，详细介绍下各个类的职责与调用关系；以及opengl 相关知识。

4. 检查工程中所有的 qml cpp hpp 代码，完善或补充注释信息，当前注释不符合要求的也要进行修改。所有注释信息参考  doxygen_comment_style.md 文件中的要求进行编写。

5. 注意更新 README.md 以及 docs/json_protocols.md 文件中的内容，确保与代码实现保持一致，以中文版本为准更新英文版本。

6. 保证最终代码可以编译通过，并正确执行。
