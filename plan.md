# 重构任务

请按如下要求重构模块：

1. 检查当前模型旋转实现，是模型转动还是相机转动，如果是模型转动是不是 camera.cpp 与实际不符合需要更换名字；
2. 模型旋转 180 度后，鼠标操作效果反向了，鼠标向下拖动模型背部却是向上旋转，我想要哪个面离我鼠标最近，这个模型面就符合鼠标拖动的方向，而不是只有“正面”；
3. UI 配色优化，当前黑色主题有些细节优化，ribbon 的 icon 都是黑色的看不清楚，Create Transform 等字体也是灰色的看不清，tab 的背景色也是看不清，需要优化；
4. 在添加一个白色主题，增加一个开关，可以切换白色主题/黑色主题；
5. 生成的代码注释使用英文。

